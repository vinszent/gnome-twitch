project('gnome-twitch', 'c', version : '0.0.1')
add_global_arguments('-std=c11', language : 'c')

LOCALEDIR = get_option('prefix') + '/' + get_option('localedir')

subdir('src')
subdir('data')
subdir('po')
